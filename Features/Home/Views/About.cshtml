@using MyWebApi.Features.Home.ViewModels
@model AboutViewModel
    
    @{
        Layout = "_Layout";
    }

<h2>Hello @Model.Author! (@Model.Year)</h2>

<ul>
    @foreach (var value in Model.Values)
    {
        <li>@value</li>
    }
</ul>